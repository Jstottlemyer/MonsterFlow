# Active prompt-emission inventory for commands/*.md
# Generated by T6 of pipeline-pacing-and-prefill spec (AC1b lock manifest).
# Format: <file>:<line>:<original prompt text>
# Scope: every active user-facing approval prompt in commands/*.md
#        excluding commands/build.md (T8 owner) and commands/preship.md (does not exist).
# Files inspected:
#   commands/spec.md, commands/spec-review.md, commands/blueprint.md, commands/check.md,
#   commands/wrap.md, commands/wrap-quick.md, commands/wrap-insights.md, commands/wrap-full.md,
#   commands/kickoff.md, commands/autorun.md, commands/flow.md
# Files with no active prompts (delegators/templates only):
#   commands/wrap-quick.md, commands/wrap-insights.md, commands/wrap-full.md,
#   commands/autorun.md, commands/flow.md
#
# After normalization, every (file, line) row below must still resolve to the active prompt
# (line numbers may shift; test uses grep against file content + a stable substring).

commands/spec.md:246:Add to roster?
commands/spec.md:330:Tags: [security*, data*, api] — Enter to accept, type list to override, or empty to skip:
commands/spec-review.md:232:Approve to proceed to /blueprint?
commands/blueprint.md:193:Approve to proceed to /check?
commands/check.md:301:Ready for /build.
commands/check.md:302:Address fixes above, then /build.
commands/wrap.md:290:Apply these updates?
commands/wrap.md:604:All loose ends handled — wrap complete?
commands/kickoff.md:149:Add these?
